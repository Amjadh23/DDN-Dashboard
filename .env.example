# Template only. Preserve existing .env.local. Never use real personal data here.
DASHBOARD_MODE=demo
DATABASE_ADMIN_URL=postgresql://dashboard_owner:REPLACE_LOCALLY@127.0.0.1:55432/dashboard
DATABASE_URL=postgresql://dashboard_app:REPLACE_LOCALLY@127.0.0.1:55432/dashboard
SESSION_SECRET=GENERATE_A_RANDOM_LOCAL_SECRET
# Optional absolute path; otherwise the latest local Windows Defender platform is discovered.
# SCANNER_PATH=C:/approved/scanner/MpCmdRun.exe
